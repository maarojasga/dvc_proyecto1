import { Container } from "@/components/ui/container";

export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-borde py-6">
      <Container>
        <p className="text-xs text-texto-suave">
          Plataforma de cursos masivos abiertos en linea — base del proyecto.
        </p>
      </Container>
    </footer>
  );
}
